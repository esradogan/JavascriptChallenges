// Create datatypes.js file and use the JavaScript typeof operator to check different data types. 
// Check the data type of each variable

console.log(typeof (2));
console.log(typeof (2.5));
console.log(typeof ('esra'));
console.log(typeof (true));
console.log(typeof (null));

// Declare four variables without assigning values

let name;
let num_Population;
let countryName;
let ıd;

// Declare four variables with assigning values

let name = 'Esra';
let num_Population = 2352452;
let countryName = 'Turkey';
let ıd = 09;

// Declare variables to store your first name, last name, marital status, country and age in multiple lines

let name = 'Esra',
    lastName = 'Doğan',
    maritalStatus = 'Single',
    country = 'Turkey',
    age = 27;
console.log(name, lastName, maritalStatus, age, country);


// Declare variables to store your first name, last name, marital status, country and age in a single line

let name = 'Esra', lastName = 'Doğan', maritalStatus = 'Single', country = 'Turkey', age = 27;
console.log(name, lastName, maritalStatus, age, country);


// Declare two variables myAge and yourAge and assign them initial values and log to the browser console

let myAge = 'I am 25 years old', yourAge = 'You are 30 years old';
console.log(myAge, yourAge);